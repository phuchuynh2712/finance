import 'package:flutter/material.dart';

/// Source colors extracted directly from the brand icon (`exports/appicon.svg`).
/// See research.md §1 for the derivation.
class AppColors {
  AppColors._();

  static const iconGreen = Color(0xFF2E8B5E);
  static const iconLightGreen = Color(0xFF8FD3AB);
  static const iconCream = Color(0xFFEBF5F0);
  static const iconGold = Color(0xFFDFB04A);
}
