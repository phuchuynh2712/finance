/// Whether the "Sign in with Google" button is shown on the sign-in and
/// registration screens. Set to `false` while Google Cloud Console / Supabase
/// Dashboard OAuth setup (research.md §4, tasks.md T002–T004) hasn't been
/// done yet — flip back to `true` once those external steps are complete.
const kGoogleSignInEnabled = false;
